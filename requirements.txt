# Minimal dependencies for the CCSF reproducibility package.
# Tested on Python 3.12. A frozen, exact-version lock file is provided in
# requirements-lock.txt; use that to reproduce the published numbers exactly.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0
transformers>=4.40
sentence-transformers>=2.2
